import subprocess

def lambda_handler(event, context):
    # Run the AWS CLI command to get the caller identity
    result = subprocess.run(['aws', 'sts', 'get-caller-identity'], stdout=subprocess.PIPE)
    
    # Parse the output to extract the user ID
    output = result.stdout.decode('utf-8')
    user_id = json.loads(output)['UserId']
    
    print(f"The current AWS user is: {user_id}")
    
    # Your function logic goes here...
